<h1>Create New Tournament</h1>

<%= form_for @tournament do |f| %>
  <div>
    <%= f.collection_check_boxes :order, Team.order(:name), :id, :name, include_hidden: false %>
  </div>
  <%= f.submit 'Submit', class: 'button' %>
<% end %>
